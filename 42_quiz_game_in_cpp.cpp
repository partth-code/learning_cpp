#include <iostream>
#include <ctime>
#include <cstdlib>

int main()
{
    srand(time(NULL));

    std::string questions[] = {"What is Python?","Which is a starter pokemon?","What is an electron","What is one chord of g scale?","which animation style was used in monty python"};

    std::string answers[] = {"","","","",""};

    return 0;
}